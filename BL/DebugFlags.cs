namespace KoobecaFeedController.BL {
    public class DebugFlags {
        //public static bool UseServerDB { get; set; } =
        //    false; //we can set it on if we debug the server DB from client machine
    }
}